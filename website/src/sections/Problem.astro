---
import FeatureCard from '../components/FeatureCard.astro';
---
<section class="section" id="problem">
  <div class="container">
    <h2 class="text-center problem__title fade-in">你的知识库为什么总是"死"的</h2>
    <div class="problem__grid fade-in">
      <FeatureCard
        icon="🔄"
        title="维护是永远的债"
        description="建文件夹、定规范、修链接——没完没了，最终放弃。"
      />
      <FeatureCard
        icon="🧩"
        title="碎片永远是碎片"
        description="同一个概念散落在五篇笔记里，每次都需要自己拼凑。"
      />
      <FeatureCard
        icon="💬"
        title="对话成果转瞬即逝"
        description="和 AI 讨论的判断和结论，聊完就丢，下次从零开始。"
      />
    </div>
  </div>
</section>

<style>
  .problem__title {
    margin-bottom: var(--space-xl);
  }

  .problem__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
  }

  @media (max-width: 900px) {
    .problem__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 600px) {
    .problem__grid {
      grid-template-columns: 1fr;
    }
  }
</style>

---
import MockupFrame from '../components/MockupFrame.astro';
import { Image } from 'astro:assets';
import healthPage from '../assets/mockups/health-page.png';
---
<section class="section section--surface">
  <div class="container grid-2 fade-in">
    <div class="feature-text">
      <h2>自动写入，但你随时能退</h2>
      <p class="text-secondary">
        每次 Agent 修改知识库，Piki 都记录完整变更日志。不满意？一键回退到上一个版本。
      </p>
      <p class="text-secondary">
        还有自动健康检查：孤立页面、断链、重复标题，Piki 帮你维护知识库的干净整洁。
      </p>
    </div>
    <div class="feature-visual">
      <MockupFrame>
        <Image src={healthPage} alt="Piki 变更日志与健康检查" />
      </MockupFrame>
    </div>
  </div>
</section>

<style>
  .feature-text h2 {
    margin-bottom: var(--space-md);
  }

  .feature-text p {
    font-size: 17px;
    line-height: 1.7;
    margin-bottom: var(--space-md);
  }

  @media (max-width: 900px) {
    .feature-visual {
      order: -1;
    }
  }
</style>

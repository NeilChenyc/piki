---
---
<section class="section" id="how-it-works">
  <div class="container text-center">
    <h2 class="fade-in">一个输入框，一个 Agent，一个本地知识库</h2>
    <p class="how__subtitle text-secondary fade-in">不分查询、写入还是整理。你说需求，Piki 判断该做什么。</p>
    <div class="how__steps fade-in">
      <div class="step">
        <div class="step__number">1</div>
        <h3 class="step__title">说你想做的事</h3>
        <p class="step__desc">自然语言或拖入文件，不需要选模式。</p>
      </div>
      <div class="step__arrow">→</div>
      <div class="step">
        <div class="step__number">2</div>
        <h3 class="step__title">Agent 读取、判断、写入</h3>
        <p class="step__desc">Claude 理解你的上下文，自动执行整理、对比、编辑。</p>
      </div>
      <div class="step__arrow">→</div>
      <div class="step">
        <div class="step__number">3</div>
        <h3 class="step__title">知识自动沉淀</h3>
        <p class="step__desc">新信息融入已有的 Wiki 页面，越用越完整。</p>
      </div>
    </div>
  </div>
</section>

<style>
  .how__subtitle {
    font-size: 18px;
    margin: var(--space-md) auto var(--space-xl);
    max-width: 500px;
  }

  .how__steps {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: var(--space-lg);
    margin-top: var(--space-lg);
  }

  .step {
    flex: 1;
    max-width: 280px;
    text-align: center;
  }

  .step__number {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: var(--color-accent-light);
    color: var(--color-accent-dark);
    font-weight: 600;
    font-size: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto var(--space-md);
  }

  .step__title {
    font-size: 17px;
    margin-bottom: var(--space-sm);
  }

  .step__desc {
    font-size: 15px;
    color: var(--color-text-secondary);
  }

  .step__arrow {
    font-size: 24px;
    color: var(--color-text-tertiary);
    margin-top: 6px;
  }

  @media (max-width: 600px) {
    .how__steps {
      flex-direction: column;
      align-items: center;
    }
    .step__arrow {
      transform: rotate(90deg);
    }
  }
</style>
